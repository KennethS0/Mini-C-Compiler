int listtest[12];

void lamaooo( const int xd, long oaksdoka12akmsd) {
    const int var = .;
    int listtest[45] = {., .};


    switch('1') {
        case '1':
            .
            break;
        default:
            .
    }

    if (.) {

    }

    else {

    }

    while (.) {
        int a = .;
        long a = .;

        for(int m=.;i<i;.){int a =.; }
        for(m=.;i<i;.){int a =.; }
        for(m=.;i==i;.){int a =.; }
        for(int m=.;i<i;.){int a =.; }
        for(int m=.;.;.){int a =.; }
        for(int m=.;.;.){}

        asdalo(.,.,.,.,.);
        //a+=42;

    }



    macacooo(.,.,.,.,.);
    macaquiño();
}